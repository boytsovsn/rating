spring:
  messages:
    encoding: UTF-8
  datasource:
    url: jdbc:mysl://localhost:3306/testRating
    username: rating_user
    password: rating_crf;bvyt
    driver-class-name: com.mysql.jdbc.Driver
  sql:
    init:
      mode: never
#      data-locations: classpath:data1.sql
#      schema-locations: classpath:schema1.sql
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

logging:
  level:
    ROOT: DEBUG