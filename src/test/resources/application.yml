spring:
  messages:
    encoding: UTF-8
  datasource:
    url: jdbc:postgresql://localhost:5432/testRating
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  sql:
    init:
      mode: never
#      data-locations: classpath:data1.sql
#      schema-locations: classpath:schema1.sql
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

logging:
  level:
    ROOT: DEBUG